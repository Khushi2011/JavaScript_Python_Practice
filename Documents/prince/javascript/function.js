console.log('This is function file');
const wish=function (name) { //function wish()  both are same work 
    console.log(`${name}`);
    return 9;
    
}
let name='Prince';
let val=wish(name);
console.log(val);
//in parameter we can provide default argument
//we can provide  object value as function
//let varible scope as block/locally
//var variable scope is globally